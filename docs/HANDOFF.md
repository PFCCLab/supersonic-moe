# SonicMoE Handoff — Redirect

> **This file is a redirect.** The canonical handoff document is at the repository root:
>
> **[`HANDOFF.md`](../HANDOFF.md)** — current session at top, prior sessions preserved verbatim below.
>
> Earlier per-phase chronicle (Sessions 1–66) is preserved in
> `reports/fp8_upgrade/engineering_log.md` (Phases 1–26).
>
> Sessions 67+ are recorded only in the root `HANDOFF.md`. Session 67 → S70 work
> (FP8 frontier IMA root-cause + bisect, ncu-driven quant kernel optimization) is
> not duplicated in the engineering log; read the corresponding sections of
> `HANDOFF.md` directly. Sessions 72–75 (race-fix + globals purge + stream patch +
> lazy main_grad + step-ordering fix + Fleet integration audit) are likewise
> root-`HANDOFF.md`-only.

